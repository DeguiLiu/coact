# coact 文档与示例交付评估任务（精简版）

**评估对象**：当前 HEAD（含 SoftIrq 接入 3e526d2、vocabulary c259b82、coro 默认构建 30bce9b 及后续文档修正）
**评估模式**：只读评估，不修改任何文件，产出中文评估报告
**前置验证**：`cd build && make -j12 && ctest`（预期 53/53）、`./examples/isp_pipeline_demo`（预期 68 断言 ALL PASS）

## 评估维度（四个，各给结论 + 证据）

1. **功能完整性**：任务清单 #32-#47 声称的交付是否都在代码中兑现。重点抽查：SoftIrq 双轨路径（`kUseSoftIrqCompletion` 门控是否真的单点、RTT 构建是否真的关闭）、vocabulary.hpp 8 类型、coro 默认 target、BitFieldView 位域层。发现声称与代码不符处逐条列出。
2. **文档可用性**：docs/example_ISP_pipeline_* 六份文档，模拟"第一次接触本项目的同事"阅读：术语是否自解释（无"保真""口径漂移"类行话残留）、图是否可渲染（可用 `tools/check_mermaid.sh` 批量验证）、断言数字（68/53）与实际运行是否一致、跨文档链接是否有效。
3. **遗留风险**：对照 review_record 第 3 章仍存在的 Major 项（RT-Thread cond 近似协议、协程 stop 孤儿、IRQ/SOUT parked 所有权不闭合、DdrCtx 非原子），评估当前 demo 场景下哪些是真实风险、哪些是防御性隐患。
4. **可复现性**：按文档复现命令从零构建运行一次（干净 build 目录），记录是否一次成功、遇到的偏差。

## 边界

- demo 定位是架构模型与故障注入演示（消息发生级），不把硬件简化当缺陷
- 不建议扩 coact 平台能力面

## 输出格式（结论先行，规模 ≤400 字/维度）

每维度：结论（1 句）→ 证据（文件:行号 或命令输出）→ 问题清单（每条含严重度与一句话建议）。最后给总体结论：**可交付 / 需修复后交付 / 需返工**。
